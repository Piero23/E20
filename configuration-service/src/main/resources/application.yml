server:
  ssl:
    enabled: true
    key-store: classpath:esse20.p12
    key-store-type: PKCS12
    key-store-password: eaproject
    key-alias: esse20
    key-password: eaproject
    protocol: TLS
    enabled-protocols: TLSv1.2
  port: 8441

spring:
  profiles:
    active: native